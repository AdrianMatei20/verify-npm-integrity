# verify-npm-integrity
Detect a patched npm CLI. Compares your npm install against the tarball npm published, verified against the registry's own integrity hash. One file, no dependencies, read-only.
